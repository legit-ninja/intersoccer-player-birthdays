# InterSoccer Player Birthdays — Cursor notes

Workspace efficiency / role workflow: see root [`AGENTS.md`](../AGENTS.md).

Follow workspace **plugin-responsibilities**, **wordpress-php**, **data-model**, and **plugin-versioning**.

Player SoT stays in Player Management (`intersoccer_get_user_players`). This plugin only reads calendar DOBs and sends guardian greetings plus an office digest. It does not own birthday-party products or RR `birthday_camp_followup`.
